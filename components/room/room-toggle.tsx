"use client";

// Room is the only dashboard view for now. 2D is kept at /dashboard/2d for
// internal reference but no toggle is surfaced in the topnav.
export function RoomToggle() {
  return null;
}
